@model SalamiTV.ViewModels.SearchProgramVM

@{
    ViewBag.Title = "Min sida";
}
<h3>
    Välkommen
    @ViewBag.UserName @HttpContext.Current.User.Identity.Name

</h3>
@Html.Partial("_partialPageNavigation", Model)
<section>
    <ul>
        <li>
            @Html.ActionLink("Lägg till en favoritkanal", "AddChannelToTablau", "UserTablau")
            <br />
            @Html.ActionLink("Ta bort en favoritkanal", "RemoveChannelFromTablau", "UserTablau")
        </li>
    </ul>
</section>

@Html.Action("PartialTvChannel", "Home", new { id = Model.AspNetUserID, page = Model.Page })