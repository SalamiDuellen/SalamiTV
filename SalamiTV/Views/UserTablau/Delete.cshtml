@model SalamiTV.Models.UserTablau

@{
    ViewBag.Title = "Ta bort kanal från tablån";//Delete
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
</head>
<body>
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>UserTablau</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.TvChannel.Name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.TvChannel.Name)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.AspNetUsersId)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.AspNetUsersId)
            </dd>

        </dl>

        @using (Html.BeginForm("Delete", "UserTablau", FormMethod.Post))
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>
</body>
</html>
